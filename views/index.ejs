<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"
        integrity="sha384-jLKHWMjoO7KTH5G7hw5R2aQuXeuEqcxXRMLkge2ly5qQEZABN5Gz1rDtr1b4uELP" crossorigin="anonymous">
    <title>Short URL</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@700&display=swap');

        body {
            padding-top: 50px;
            background: #000000;
            font-family: 'Poppins', sans-serif;
            color: #ffffff;
            overflow-x: hidden;
        }

        h1 {
            font-size: 3rem;
            background: linear-gradient(90deg, #ff0057, #007bff, #ff0057, #007bff);
            background-size: 400%; /* Increased size to allow smoother transition */
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: 700;
            text-align: center;
            margin-bottom: 1.5rem;
            animation: neon-move 10s ease infinite; /* Increased duration for a smoother loop */
        }
        
        @keyframes neon-move {
            0% {
                background-position: 0%;
            }
            50% {
                background-position: 100%;
            }
            100% {
                background-position: 0%;
            }
        }
        
        
        
        

        .container {
            background: rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0px 0px 20px rgba(255, 0, 87, 0.5);
            backdrop-filter: blur(10px);
            margin-top: 30px;
        }
        
        

        @keyframes fade-in {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .form-control {
            border: 2px solid #007bff;
            box-shadow: 0px 0px 15px rgba(0, 123, 255, 0.5);
            background: rgba(255, 255, 255, 0.8);
            color: #000000;
        }

        .btn-primary {
            background: linear-gradient(90deg, #ff0057, #007bff);
            border: none;
            padding: 10px 30px;
            font-size: 1.2rem;
            font-weight: 700;
            box-shadow: 0px 0px 15px rgba(255, 0, 87, 0.5);
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            transform: scale(1.05);
            box-shadow: 0px 0px 25px rgba(255, 0, 87, 0.75);
        }

        .table {
            background: rgba(0, 0, 0, 0.8);
            color: #ffffff;
            border-radius: 10px;
            overflow: hidden;
        }

        .table thead th {
            background: #007bff;
            color: #ffffff;
            text-align: center;
            border-bottom: none;
        }

        .table tbody tr {
            transition: all 0.3s ease;
        }

        .table tbody tr:hover {
            background: rgba(0, 123, 255, 0.2);
        }

        .table td {
            text-align: center;
        }

        .table td a {
            color: #ff0057;
            font-weight: 700;
            text-decoration: none;
        }

        .table td a:hover {
            color: #ff4081;
            text-decoration: underline;
        }

        .text-muted {
            color: #ffffff !important;
            text-align: center;
            margin-top: 20px;
            font-size: 1.1rem;
            text-shadow: 0 0 10px #007bff;
        }

        .input-group-append .btn {
            padding-left: 30px;
            padding-right: 30px;
        }

        @media (max-width: 768px) {
            .container {
                padding: 20px;
            }

            h1 {
                font-size: 2rem;
            }

            .input-group {
                flex-direction: column;
                align-items: stretch;
            }

            .input-group-append .btn {
                margin-top: 10px;
                width: 100%;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="text-primary">URL Shortener</h1>
        <form action="/shortUrls" method="POST" class="my-4 form-inline justify-content-center">
            <div class="input-group">
                <input required placeholder="Enter the link here" type="url" name="fullUrl" id="fullUrl"
                    class="form-control form-control-lg rounded-left" style="max-width: 100%;">
                <div class="input-group-append">
                    <button class="btn btn-primary btn-lg rounded-right" type="submit"><i
                            class="fas fa-magic"></i> Shorten URL</button>
                </div>
            </div>
        </form>
        <p class="text-muted">ShortURL is a free tool to shorten URLs and generate short links.</p>
        <p class="text-muted">URL shortener allows you to create a shortened link making it easy to share.</p>
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Full URL</th>
                        <th>Short URL</th>
                        <th>Clicks</th>
                    </tr>
                </thead>
                <tbody>
                    <% shortUrls.forEach(shortUrl => { %>
                    <tr>
                        <td><a href="<%= shortUrl.full %>" target="_blank" rel="noopener noreferrer"><%= shortUrl.full %></a></td>
                        <td><a href="<%= shortUrl.short %>" target="_blank" rel="noopener noreferrer"><%= shortUrl.short %></a></td>
                        <td><%= shortUrl.clicks %></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>

</html>
